#pragma once

#include <string>

void printFunc(const std::string& str,
               const std::string& name);

void printFunc(const std::string& str,
               int i,
               int result,
               const std::string& name);

void printFunc(int i,
               int j,
               const std::string& name);
