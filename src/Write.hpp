#pragma once

#include "Book.hpp"

void writeToBook(const std::string& name, 
                 const std::string& answer, 
                 int result, 
                 const int64_t& period, 
                 Book& book);
